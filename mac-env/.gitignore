# Pixi-built environment (multi-GB, regenerated by `pixi install`)
.pixi/

# colcon workspace artifacts (regenerated by `colcon build`)
ws/build/
ws/install/
ws/log/

# Smoke-test outputs (written to /tmp by default, but guard anyway)
outputs/

# Python
__pycache__/
*.py[cod]
*.egg-info/
